{% for category in categories %}
    <a class="dropdown-item" href="{{ path('category_show', { 'id': category.id}) }}" style="color: lightslategrey">
        {{ category.name }}
    </a>
{% endfor %}
