<nav class="navbar navbar-expand-lg">
    <a class="navbar-brand" href="/"><i class="fas fa-film"></i> Wild Séries</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('wild_index') }}">Séries</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Acteurs</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Mes Séries</a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">
                    Admin
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('program_index') }}">Séries</a>
                    <a class="dropdown-item" href="{{ path('season_index') }}">Saisons</a>
                    <a class="dropdown-item" href="{{ path('episode_index') }}">Episodes</a>
                    <a class="dropdown-item" href="{{ path('actor_index') }}">Acteurs</a>
                </div>
            </li>
            <li class="nav-item">
                <div class="m-2">
                    {% if app.user %}
                        {{ app.user.email }}
                        <a href="{{ path('app_logout') }}">Se déconnecter <i class="fas fa-user"></i></a>
                    {% else %}
                        <a href="{{ path('app_login') }}">Se connecter <i class="fas fa-user"></i></a>
                    {% endif %}
                </div>

            </li>
        </ul>
    </div>
</nav>
